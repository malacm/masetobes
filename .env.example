# Sanity — public values, exposed to the browser
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Sanity — server-only token, used for previews/drafts (optional for v1)
SANITY_API_READ_TOKEN=

# Resend — server-only
RESEND_API_KEY=
RESEND_FROM_EMAIL=onboarding@resend.dev
CONTACT_TO_EMAIL=
